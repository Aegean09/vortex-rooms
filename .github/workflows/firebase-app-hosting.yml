# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase App Hosting
'on':
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Needed for authentication with Google Cloud
    steps:
      - uses: actions/checkout@v4
      - name: 'Deploy to App Hosting'
        uses: firebase/firebase-apphosting-deploy@v0
        with:
          project_id: 'studio-538325307-b0443'
          site_id: 'vortex-4b5b7' # The ID of your App Hosting site
          # You can specify a different location for your backend if needed.
          # The default is us-central1.
          # location: us-central1
          # If you have a different service account you'd like to use, you can specify it here.
          # service_account: ''
          # The amount of time to wait for the backend to be deployed before considering it a failure.
          # The default is 20m.
          # wait_for_backend: 20m
          # If you want to specify an explicit branch to deploy from, you can set it here.
          # By default, it uses the branch that triggered the workflow.
          # branch: ''
          # The number of failed rollouts to keep. Default is 0
          # failed_rollouts_to_keep: 0
